import React, {Fragment} from 'react'
import {Header, Navigation, Main, Footer} from '../../components'

const App = () => (
	
	<Fragment>
		<Header/>
		<Navigation/>
		<Main/>
		<Footer/>
	</Fragment>
)

export {App}